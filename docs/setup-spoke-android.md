<!-- As Above, So Below. As Within, So Without. The Future Dictates the Past and the Past is Always Present. -->
# Spoke setup — RETIRED (Phase 22)

The Flutter spoke (`spoke/`) was deleted when the native Kotlin client
(`spoke_kt/`) became the definitive frontend. This file is kept so old links
land somewhere honest.

Build the native app: open `spoke_kt/` in Android Studio, press Run, then
**Setup → Tether** and enter `http://<laptop-LAN-IP>:8000`.
